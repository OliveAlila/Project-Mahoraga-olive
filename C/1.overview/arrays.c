#include <stdio.h>

int main(int argc, char const *argv[])
{
    char numArray[20] = {
        'a',
        'b',
        'c',
        'd',
        'e',
    };

    printf("%c", numArray[0]);
    printf("%c", numArray[1]);
    printf("%c", numArray[2]);
    printf("%c", numArray[3]);
    printf("%c", numArray[4]);

    return 0;

}
